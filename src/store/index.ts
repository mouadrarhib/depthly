/**
 * Re-export all stores from one place.
 * Import stores directly from here in components.
 *
 * Usage:
 *   import { useUiStore, useAuthStore } from '@/store'
 */
export { useUiStore }   from './uiStore'
export { useAuthStore } from './authStore'
